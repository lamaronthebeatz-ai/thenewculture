__pycache__/
.pytest_cache/

# Phase 4 CLI runtime state (scripts/editorial.py) — not part of the
# architecture, just lets separate CLI invocations see the same
# StoryCandidates; safe to delete any time, regenerated by `collect`.
editorial-intelligence/.cli-state/

# Phase 4 CLI markdown output (scripts/editorial.py `markdown` command)
draft-*.md

# Cloudflare Worker (workers/cms-auth)
.wrangler/
.dev.vars
node_modules/

